
<% @hotel.comments.each do |comment| %>
<p><%= comment.created_at %></p>
    <p>
      <strong>Evaluation:</strong>
      <%= comment.evaluation %>
    </p>
    <p>
      <strong>Comment:</strong>
      <%= comment.content %>
    </p>
<% end %>

